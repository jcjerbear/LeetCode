#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    int missingNumber(vector<int> &nums) {

    }
};

int main() {
    Solution result;
    int input[] = {1, 2, 3, 5};
    vector<int> inputVector(input, input + sizeof(input) / sizeof(int));
    result.missingNumber(inputVector);
    return 0;
}