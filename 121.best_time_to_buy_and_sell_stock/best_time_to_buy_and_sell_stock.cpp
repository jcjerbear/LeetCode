#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    int maxProfit(vector<int> &prices) {

    }
};

int main() {
    Solution result;
    int input[] = {7, 1, 5, 3, 6, 4};
    vector<int> inputVector(input, input + sizeof(input) / sizeof(int));
    result.maxProfit(inputVector);
    return 0;
}